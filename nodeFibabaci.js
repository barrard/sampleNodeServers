function findSupport(high, low){
	delta = (high-low)
	primary  = high - (delta * .381)
	secondary = high - (delta* .618)
	console.log('From '+high+' to '+low+ ' : we have the following support predictions')
	console.log('Primary support is at '+primary)
	console.log('secondary support is at '+secondary)
}

function findNewHigh(newHighGuess, low){

	newHigh = 


	console.log('if '+newHighGuess+' the new supports would be would be ')
}